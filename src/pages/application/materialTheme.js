/* eslint-disable */
import React from 'react';
import { createMuiTheme } from 'material-ui/styles';

// this block for all material ui theme color and settings.
export const Theme = createMuiTheme({
  palette: {
    primary: {
      light: '#000361',
      main: '#000361',
      dark: '#002884',
      contrastText: '#fff',
    },
    secondary: {
      light: '#ff7961',
      main: '#f44336',
      dark: '#ba000d',
      contrastText: '#000',
    },
  },
});
